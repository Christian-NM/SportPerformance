<UserControl x:Class="SportPerformance.Views.EntrenamientosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Historial de Entrenamientos" Style="{StaticResource TituloStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Fecha:" Style="{StaticResource LabelStyle}"/>
            <DatePicker SelectedDate="{Binding NuevaFecha, UpdateSourceTrigger=PropertyChanged}" Width="110"/>

            <TextBlock Text="Comentario:" Style="{StaticResource LabelStyle}"/>
            <TextBox Width="150" Text="{Binding NuevoComentario, UpdateSourceTrigger=PropertyChanged}"/>

            <Button Content="Nuevo" Command="{Binding GuardarEntrenamientoCommand}" Width="70"/>
            <Button Content="Actualizar" Command="{Binding ActualizarEntrenamientoCommand}" Width="80"/>
            <Button Content="Eliminar" Command="{Binding EliminarEntrenamientoCommand}" Width="80" Style="{StaticResource BotonEliminarStyle}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding ListaEntrenamientos}" SelectedItem="{Binding EntrenamientoSeleccionado}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy}" Width="100"/>
                <DataGridTextColumn Header="Comentario" Binding="{Binding Comentario}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>