<UserControl x:Class="SportPerformance.Views.SeriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Gestión de Series" Style="{StaticResource TituloStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Ejercicio:" Style="{StaticResource LabelStyle}"/>
            <ComboBox ItemsSource="{Binding ListaEjercicios}" SelectedItem="{Binding EjercicioSeleccionado}" DisplayMemberPath="Nombre" Width="120" Margin="5,0,15,0"/>

            <TextBlock Text="Entrenamiento:" Style="{StaticResource LabelStyle}"/>
            <ComboBox ItemsSource="{Binding ListaEntrenamientos}" SelectedItem="{Binding EntrenamientoSeleccionado}" DisplayMemberPath="Fecha" ItemStringFormat="dd/MM/yyyy" Width="120" Margin="5,0,15,0"/>

            <TextBlock Text="Peso (kg):" Style="{StaticResource LabelStyle}"/>
            <TextBox Width="50" Text="{Binding NuevoPeso, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,15,0"/>

            <TextBlock Text="Reps:" Style="{StaticResource LabelStyle}"/>
            <TextBox Width="50" Text="{Binding NuevasRepeticiones, UpdateSourceTrigger=PropertyChanged}" Margin="5,0,15,0"/>

            <Button Content="Guardar" Command="{Binding GuardarCommand}" Width="70" Margin="0,0,5,0"/>
            <Button Content="Eliminar" Command="{Binding EliminarCommand}" Width="80" Background="#E74C3C" Foreground="White" BorderThickness="0"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding ListaSeries}" SelectedItem="{Binding SerieSeleccionada}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="ID Ejercicio" Binding="{Binding IdEjercicio}" Width="80"/>
                <DataGridTextColumn Header="ID Entreno" Binding="{Binding IdEntrenamiento}" Width="80"/>
                <DataGridTextColumn Header="Peso" Binding="{Binding Peso}" Width="80"/>
                <DataGridTextColumn Header="Repeticiones" Binding="{Binding Repeticiones}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>