<UserControl x:Class="SportPerformance.Views.SeriesView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Background="White">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Registro de Series" Style="{StaticResource TituloStyle}"/>

        <StackPanel Grid.Row="1" Orientation="Horizontal" Margin="0,0,0,20">
            <TextBlock Text="Sesión:" Style="{StaticResource LabelStyle}"/>
            <ComboBox ItemsSource="{Binding ListaEntrenamientos}" SelectedItem="{Binding EntrenamientoSeleccionado}" DisplayMemberPath="Fecha" Width="100"/>

            <TextBlock Text="Ejercicio:" Style="{StaticResource LabelStyle}"/>
            <ComboBox ItemsSource="{Binding ListaEjercicios}" SelectedItem="{Binding EjercicioSeleccionado}" DisplayMemberPath="Nombre" Width="100"/>

            <TextBlock Text="Peso:" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Peso, UpdateSourceTrigger=PropertyChanged}" Width="40"/>

            <TextBlock Text="Reps:" Style="{StaticResource LabelStyle}"/>
            <TextBox Text="{Binding Repeticiones, UpdateSourceTrigger=PropertyChanged}" Width="40"/>

            <Button Content="Nuevo" Command="{Binding GuardarSerieCommand}" Width="60"/>
            <Button Content="Actualizar" Command="{Binding ActualizarSerieCommand}" Width="70"/>
            <Button Content="Eliminar" Command="{Binding EliminarSerieCommand}" Width="70" Style="{StaticResource BotonEliminarStyle}"/>
        </StackPanel>

        <DataGrid Grid.Row="2" ItemsSource="{Binding ListaSeries}" SelectedItem="{Binding SerieSeleccionada}" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="50"/>
                <DataGridTextColumn Header="Fecha Sesión" Binding="{Binding EntrenamientoInfo}" Width="100"/>
                <DataGridTextColumn Header="Ejercicio" Binding="{Binding EjercicioNombre}" Width="150"/>
                <DataGridTextColumn Header="Peso (kg)" Binding="{Binding Peso}" Width="80"/>
                <DataGridTextColumn Header="Repeticiones" Binding="{Binding Repeticiones}" Width="80"/>
            </DataGrid.Columns>
        </DataGrid>
    </Grid>
</UserControl>